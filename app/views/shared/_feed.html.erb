<%= link_to "New order..", 'orders/new'%>
<% if @feed_items.any? %>
    <table class="orders" summery="Status feed">
      <%= render :partial => 'shared/feed_item', :collection => @feed_items %>
    </table>

    <%= will_paginate @feed_items %>

<% end %>